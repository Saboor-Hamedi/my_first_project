I see what you have done That's not the lower OK Blur is not like that you see the new vim or the window terminal It has BL The window itself becomes kind of blur OK right now the text the editor the sidebar they all have its own color Only the window which is behind these things becomes transparent not blur and what are those three options you added Remove them You have to make accent and the BL score so move move it in accent color so it's easy access it for the carrot's names right we have it that carried cards do not have like typography and look like we have their border right It doesn't have that border when we select So yeah you fix that one
