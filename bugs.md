Well what you do is your to go apply this one we can import from our vault I have Obsidian vault I have like thousands of in the format there what folders what I do is drag them all into mine forge and it embeds them all in the database OK Extract the fold do not insert the folder if any other file like PDF you allow text as well Dot text and dot indie should be allowed and the rest should be ignored silently in the background It should be very robust because I'm....'m gonna drag and ball like thousands Thousands of folders full of MD files and text files so it should be very smooth Put it on the should show all the details all the stats like the size the remaining How many fil inserted answer on SO it should not break it should be robust it should be clear it should be great And at the end once it's clear yeah there is a cancel button we clear everything If you close the model it should not cancel it the Gage should run in the background unless we hit the cancel button it cancels that one go ahead apply that one without making any implementation plan so make sure keep it very great and robust.
