//! Pending-work state descriptors and helper state types for VimEngine.

// Holds any helper types that describe pending work (counts, operator scope, prefix, register prefix).
// Kept minimal to preserve exact VimEngine field layouts and public interfaces.
